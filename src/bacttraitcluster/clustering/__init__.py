from .consensus import (
    consensus_matrix as consensus_matrix,
    consensus_labels as consensus_labels,
    stability_path as stability_path,
    select_k as select_k,
    variation_of_information as variation_of_information,
    register_algorithm as register_algorithm,
)
